---
title: "CppCon 2026, what actually landed"
slug: "cppcon-2026-recap"
pubDate: 2026-09-20
author: filip-sajdak
language: en
kind: short
audience: working-cpp
tags: [news, conferences, cppcon, community, cpp]
summary: "Placeholder reserving the slot for post-conference coverage of CppCon 2026, which runs 12 to 18 September in Aurora, Colorado. Replace the body and set draft to false before 19 September."
draft: true
relatedPosts: [cppcon-2026-onsite-only, cppcon-2026-keynote-panel]
---

This post is a reserved slot, not finished work. It is `draft: true`, so it is
excluded from the production build and from `npm run check:future`, and it will not
be picked up by the Buffer scheduler.

CppCon 2026 runs 12 to 18 September in Aurora, Colorado, with no online track. The
schedule here was already full every day through 22 October, so without holding this
date the earliest possible recap would have been 23 October, five weeks after the
event closed.

## Before 19 September

Replace this body with the actual coverage, then set `draft: false` and render the
cards. Things worth pulling from the conference, in rough order of how well they fit
what gets written here:

- Stroustrup's opening Monday talk, and whether the profiles timeline moves again.
- Anything concrete on contracts, given the removal dispute covered on 30 August is
  still open going into Búzios in November.
- Talks with a reproducible core, which is what turns a recap into something with a
  Compiler Explorer link rather than a list of titles.
- Announcements of the kind that do not show up in release notes: implementation
  status, vendor commitments, timelines.

Two of the previews already published are `cppcon-2026-keynote-panel` and
`cppcon-2026-onsite-only`, so this should read as the follow-through on those rather
than starting over.

If the conference produces nothing worth a post, delete this file. An empty day costs
one social slot and nothing else.
