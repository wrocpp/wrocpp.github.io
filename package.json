{
  "name": "wrocpp-github-io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Wroclaw C++ community hub — blog, events, resources.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "python3 scripts/check-llms-sync.py && python3 scripts/prose-lint.py --all --strict",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "check:llms-sync": "python3 scripts/check-llms-sync.py",
    "check:prose": "python3 scripts/prose-lint.py --all --strict"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.2",
    "js-yaml": "^5.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@tailwindcss/vite": "^4.3.1",
    "@types/js-yaml": "^4.0.9",
    "tailwindcss": "^4.3.1",
    "typescript": "^5.6.0"
  },
  "overrides": {
    "yaml": "^2.8.3"
  }
}
