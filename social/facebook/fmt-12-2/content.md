---
template: social/linkedin-post
---

::::post{type=insight theme=dark logo=top-left}

:::insight{citation="wro.cpp -- 2026-08-12"}
# A format call that costs nothing at runtime
FMT_COMPILE parses the format string during compilation, so the whole format can be a constant expression. Version 12.2 adds a C API and a module target.
:::

::::
