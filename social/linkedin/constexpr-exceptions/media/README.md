# Media directory

Drop images here (PNG, JPG, SVG) for use in your post.

Reference them in your content.md with:

    :::image{src="screenshot.png" alt="Alt text" position="top"}

The renderer resolves paths relative to this media/ directory.
