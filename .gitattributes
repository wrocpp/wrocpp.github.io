# intentionally empty: brand PNGs are small and shipped as plain blobs.
# (No `*.png filter=lfs ...` line: GitHub Pages serves the raw blob,
# which for LFS-tracked files is the pointer text, not the bytes.)
